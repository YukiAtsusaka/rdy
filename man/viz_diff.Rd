% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_diff.R
\name{viz_diff}
\alias{viz_diff}
\title{Visualize effects from \code{diff_in_means()}}
\usage{
viz_diff(
  estimates,
  scale = c("unit", "raw", "std"),
  layout = c("facets", "single_panel"),
  palette = c(Negative = "#b0015a", Not_significant = "gray70", Positive = "#128ba0"),
  point_size = 2,
  line_width = 0.5
)
}
\arguments{
\item{estimates}{Tibble returned by \code{diff_in_means()} (long form) with
columns including: \code{estimate}, \code{std.error}, \code{conf.low},
\code{conf.high}, \code{scale}, \code{outcome}, \code{effect_type}, \code{item}, etc.}

\item{scale}{Which scale to plot: \code{"unit"} (default), \code{"raw"}, or \code{"std"}.}

\item{layout}{\code{"facets"} (default) to facet by effect type and item, or
\code{"single_panel"} for one panel per item with separators between effect blocks.}

\item{palette}{Named colors for \code{c("Negative","Not_significant","Positive")}.}

\item{point_size}{Point size (default 2).}

\item{line_width}{Line width for CI bars (default 0.5).}
}
\description{
Produces a dot-and-interval plot of treatment effects, with configurable layout.
}
\details{
Average rank and pairwise ranking labels are alphabetically ordered (after
stripping prefixes), top-k and marginal are ordered numerically.
}
